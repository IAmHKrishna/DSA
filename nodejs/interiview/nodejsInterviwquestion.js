// Senior Backend Node Software Engineer
//  resolves or rejects as soon as one of the promises in the array resolves or rejects,
//  with the value or reason from that promise.
//  This means that `Promise.race` is useful for scenarios where you want to take action as soon as one of the promises completes,
//  while `Promise.all` is useful when you want to wait for all promises to complete before proceeding.
